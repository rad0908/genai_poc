# Ignore everything inside output/ but keep the folder with a placeholder
/output/*
!/output/.gitkeep

# Ignore only root-level HTML files
/*.html

# Ignore any directory whose name starts with "venv" (venv, venv3.11, venv_new, etc.)
venv*/

# Ignore Python cache for src
__pycache__/